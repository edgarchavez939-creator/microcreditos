# URL pública del backend en Render (incluye /api al final)
VITE_API_URL=https://TU-SERVICIO.onrender.com/api
# Clave de Google Maps (restringida por dominio)
VITE_GOOGLE_MAPS_API_KEY=

# Mismo ID de cliente OAuth que el backend (GOOGLE_CLIENT_ID).
VITE_GOOGLE_CLIENT_ID=
